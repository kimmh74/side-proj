<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kmh.kamco.mapper.UserMapper">

    <select id="findByUsername" resultType="com.kmh.kamco.model.User">
        SELECT
            id,
            username,
            password,
            email, <!-- email 컬럼 추가 -->
            name,  <!-- name 컬럼 추가 -->
            created_at AS createdAt
        FROM
            userdb
        WHERE
            username = #{username}
    </select>

    <select id="existsByUsername" resultType="boolean">
        SELECT EXISTS (
            SELECT 1
            FROM userdb
            WHERE username = #{username}
        )
    </select>
    
    <!-- ★★★ email 존재 여부 확인 쿼리 추가 ★★★ -->
    <select id="findByEmail" resultType="com.kmh.kamco.model.User">
        SELECT
            id,
            username,
            password,
            email,
            name,
            created_at AS createdAt
        FROM
            userdb
        WHERE
            email = #{email}
    </select>

    <select id="existsByEmail" resultType="boolean">
        SELECT EXISTS (
            SELECT 1
            FROM userdb
            WHERE email = #{email}
        )
    </select>

    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO userdb (username, password, email, name, created_at)
        VALUES (#{username}, #{password}, #{email}, #{name}, #{createdAt})
    </insert>

    <!-- ★★★ 사용자 정보 업데이트 쿼리 추가 ★★★ -->
    <update id="updateUser">
        UPDATE userdb
        SET
            password = #{password},
            email = #{email},
            name = #{name}
        WHERE
            id = #{id}
    </update>

</mapper>